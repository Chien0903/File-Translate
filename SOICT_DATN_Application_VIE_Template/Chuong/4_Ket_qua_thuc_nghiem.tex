\documentclass[../DoAn.tex]{subfiles}
\begin{document}

% Agent: BEGIN
\textbf{Tổng quan Chương 4.} Chương 3 đã trình bày lý do lựa chọn và vai trò của từng nhóm công nghệ. Chương 4 này áp dụng những công nghệ đó vào việc phân tích, thiết kế và xây dựng hệ thống dịch đa ngôn ngữ. Nội dung trình bày theo trình tự từ kiến trúc tổng thể, thiết kế chi tiết giao diện và cơ sở dữ liệu, đến kết quả xây dựng, kiểm thử và triển khai. Hệ thống được thiết kế theo kiến trúc MVC với frontend tách rời, trong đó React SPA đảm nhiệm View, Django REST Framework đảm nhiệm Controller và Django ORM đảm nhiệm Model.
% Agent: END

\section{Thiết kế kiến trúc}
\subsection{Lựa chọn kiến trúc phần mềm}
Mục này có độ dài từ một đến ba trang. Sinh viên cần lựa chọn kiến trúc phần mềm cho ứng dụng của mình như: kiến trúc ba lớp MVC, MVP, SOA, Microservice, v.v. rồi giải thích sơ bộ về kiến trúc đó (không giải thích chi tiết/dài dòng).
Sử dụng kiến trúc phần mềm đã chọn ở trên, sinh viên mô tả kiến trúc cụ thể cho ứng dụng của mình. Gợi ý: sinh viên áp dụng lý thuyết chung vào hệ thống/sản phẩm của mình như thế nào, có thay đổi, bổ sung hoặc cải tiến gì không. Ví dụ, thành phần M trong kiến trúc lý thuyết MVC sẽ là những thành phần cụ thể nào (ví dụ: là interface I + class C1 + class C2, v.v.) trong kiến trúc phần mềm của sinh viên.
\textbf{a, Lựa chọn kiến trúc: MVC với frontend tách rời}

Trong đồ án này, kiến trúc MVC (Model -- View -- Controller) với frontend tách rời được lựa chọn để xây dựng hệ thống dịch tài liệu đa ngôn ngữ. Đây là biến thể thường gặp trong các ứng dụng web hiện đại, còn có thể gọi là API-driven MVC. Khác với MVC truyền thống, nơi View thường được render ở phía server bằng template, hệ thống này đặt View ở phía client thông qua React SPA. Frontend lấy dữ liệu từ backend bằng REST API và tự render giao diện trên trình duyệt.

Về bản chất, hệ thống vẫn tuân theo tư tưởng chính của MVC: Model quản lý dữ liệu, Controller xử lý yêu cầu và điều phối nghiệp vụ, còn View hiển thị dữ liệu cho người dùng. Cách tổ chức này giúp tách rõ trách nhiệm giữa giao diện người dùng, xử lý nghiệp vụ và quản lý dữ liệu, đồng thời phù hợp với định hướng xây dựng frontend và backend độc lập.

\textbf{b, Model (M) -- Xử lý dữ liệu và nghiệp vụ ứng dụng}

Model là nơi quản lý dữ liệu, thực hiện các thao tác đọc/ghi vào cơ sở dữ liệu và đảm bảo các ràng buộc dữ liệu chính của hệ thống. Trong đồ án, Model được hiện thực bằng Django ORM. Các model đóng vai trò biểu diễn dữ liệu nghiệp vụ, giúp backend thao tác với cơ sở dữ liệu thông qua lớp đối tượng Python thay vì viết trực tiếp các câu lệnh SQL trong từng chức năng.

Trong hệ thống dịch tài liệu đa ngôn ngữ, Model đảm nhiệm:
\begin{itemize}
  \item Định nghĩa các thực thể dữ liệu như người dùng, công ty, ngôn ngữ, tệp đã dịch, thuật ngữ cá nhân, đề xuất thuật ngữ, thông báo và refresh token.
  \item Lưu trữ trạng thái nghiệp vụ như vai trò người dùng, lịch sử dịch, trạng thái đề xuất thuật ngữ và cấu hình ngôn ngữ theo công ty.
  \item Thiết lập quan hệ dữ liệu giữa người dùng, công ty, tệp dịch, thuật ngữ và các bản ghi cấu hình.
  \item Hỗ trợ backend truy xuất và cập nhật dữ liệu thông qua Django ORM.
\end{itemize}

\textbf{c, View (V) -- Giao diện người dùng}

View chịu trách nhiệm hiển thị dữ liệu và cung cấp giao diện tương tác giữa người dùng và hệ thống. Trong kiến trúc của đồ án, View không được render bởi Django template ở phía server mà được hiện thực bằng React SPA ở phía client. View nhận dữ liệu từ Controller thông qua REST API, sau đó hiển thị kết quả trên trình duyệt và gửi các thao tác tiếp theo của người dùng về backend.

Trong hệ thống, View đảm nhiệm:
\begin{itemize}
  \item Xây dựng các màn hình giao diện cho người dùng và quản trị viên.
  \item Hiển thị dữ liệu trả về từ backend như kết quả dịch, lịch sử tệp, danh sách thuật ngữ, thông báo và thông tin cấu hình.
  \item Thu thập thao tác của người dùng như nhập văn bản, chọn tệp, chọn ngôn ngữ, gửi đề xuất thuật ngữ hoặc thực hiện thao tác quản trị.
  \item Giao tiếp với Controller thông qua các service gọi API.
\end{itemize}

\textbf{d, Controller (C) -- Cầu nối xử lý giữa người dùng và hệ thống}

Controller đóng vai trò trung gian giữa View và Model. Controller tiếp nhận yêu cầu từ frontend, kiểm tra dữ liệu đầu vào, xác thực và phân quyền người dùng, sau đó tương tác với Model hoặc các dịch vụ bên ngoài để xử lý nghiệp vụ. Kết quả xử lý được trả về cho View dưới dạng JSON để frontend hiển thị.

Trong đồ án, Controller được hiện thực bằng Django views và Django REST Framework endpoints. Các REST endpoints xử lý các nhóm nghiệp vụ như xác thực, dịch văn bản, dịch tệp tài liệu, quản lý lịch sử dịch, quản lý thuật ngữ, thông báo và cấu hình ngôn ngữ. Ngoài tương tác với Model, Controller còn điều phối các dịch vụ ngoài như Google Cloud Translation, Google Cloud Storage, Amazon S3 và ConvertAPI khi cần.

\subsection{Thiết kế tổng quan}
Sinh viên vẽ biểu đồ gói UML (UML package diagram), nêu rõ sự phụ thuộc giữa các gói (package). SV cần vẽ các gói sao cho chúng được phân theo các tầng rõ ràng, không được sắp đặt package lộn xộn trong hình vẽ. Sinh viên chú ý các quy tắc thiết kế (Các gói không phụ thuộc lẫn nhau, gói tầng dưới không phụ thuộc gói tầng trên, không phụ thuộc bỏ qua tầng, v.v.) và cần giải thích sơ lược về mục đích/nhiệm vụ của từng package. SV tham khảo ví dụ minh họa trong Hình \ref{fig:Fig1}

Agent:

Biểu đồ gói tổng quan của hệ thống được thiết kế theo hướng đơn giản, gồm bốn gói chính: \texttt{frontend}, \texttt{backend}, \texttt{database} và \texttt{external\_services}. Biểu đồ được trình bày trong Hình~\ref{fig:Fig1}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Hinhve/Package.png}
    \caption{Biểu đồ gói tổng quan của hệ thống}
    \label{fig:Fig1}
\end{figure}

\textbf{a, Gói frontend}

\textbf{Mục đích:} Đây là gói chịu trách nhiệm hiển thị giao diện người dùng và tiếp nhận các thao tác từ người dùng. Gói này được xây dựng bằng React, đóng vai trò View trong kiến trúc MVC với frontend tách rời.

\textbf{Nhiệm vụ:}
\begin{itemize}
  \item Hiển thị các màn hình chức năng của hệ thống.
  \item Thu thập thao tác người dùng như nhập văn bản, chọn tệp, chọn ngôn ngữ và gửi yêu cầu xử lý.
  \item Gửi các yêu cầu API đến backend và hiển thị dữ liệu backend trả về.
  \item Không truy cập trực tiếp vào database hoặc các dịch vụ ngoài.
\end{itemize}

\textbf{b, Gói backend}

\textbf{Mục đích:} Đây là gói trung tâm xử lý logic nghiệp vụ của hệ thống. Backend được xây dựng bằng Django và Django REST Framework, đóng vai trò Controller trong kiến trúc MVC.

\textbf{Nhiệm vụ:}
\begin{itemize}
  \item Tiếp nhận request từ frontend thông qua REST API.
  \item Xác thực, phân quyền và kiểm tra dữ liệu đầu vào.
  \item Điều phối các xử lý nghiệp vụ như dịch văn bản, dịch tệp, quản lý thuật ngữ và lưu lịch sử.
  \item Giao tiếp với database và các dịch vụ ngoài khi cần.
\end{itemize}

\textbf{c, Gói database}

\textbf{Mục đích:} Đây là gói quản lý dữ liệu bền vững của hệ thống. Gói này tương ứng với phần Model trong kiến trúc MVC, được truy cập thông qua Django ORM.

\textbf{Nhiệm vụ:}
\begin{itemize}
  \item Lưu trữ dữ liệu người dùng, tệp đã dịch, ngôn ngữ, thuật ngữ, đề xuất thuật ngữ và thông báo.
  \item Đảm bảo dữ liệu được truy xuất và cập nhật thông qua backend.
  \item Không giao tiếp trực tiếp với frontend.
\end{itemize}

\textbf{d, Gói external\_services}

\textbf{Mục đích:} Đây là gói đại diện cho các dịch vụ bên ngoài mà hệ thống tích hợp để thực hiện những tác vụ không tự xử lý trong lõi ứng dụng.

\textbf{Nhiệm vụ:}
\begin{itemize}
  \item Cung cấp dịch vụ dịch máy thông qua Google Cloud Translation.
  \item Lưu trữ glossary thông qua Google Cloud Storage.
  \item Lưu trữ tệp thông qua Amazon S3.
  \item Chuyển đổi định dạng tài liệu thông qua ConvertAPI.
\end{itemize}

\textbf{e, Mối quan hệ giữa các gói}

Gói \texttt{frontend} phụ thuộc vào \texttt{backend} để gửi yêu cầu và nhận dữ liệu hiển thị. Gói \texttt{backend} phụ thuộc vào \texttt{database} để đọc/ghi dữ liệu nghiệp vụ. Đồng thời, \texttt{backend} phụ thuộc vào \texttt{external\_services} để gọi các dịch vụ dịch máy, lưu trữ tệp, quản lý glossary và chuyển đổi định dạng. Các gói \texttt{database} và \texttt{external\_services} không phụ thuộc ngược lại vào frontend hoặc backend, giúp kiến trúc tổng quan rõ ràng và dễ bảo trì.

\subsection{Thiết kế chi tiết gói}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{Hinhve/4.1.3.png}
    \caption{Biểu đồ gói chi tiết của hệ thống}
    \label{fig:package-detail}
\end{figure}

Hệ thống được chia thành hai phần chính là \texttt{frontend} và \texttt{backend}, tuân thủ hướng MVC với frontend tách rời. Ngoài ra, hệ thống còn có gói \texttt{Translate\_v2} phục vụ xử lý dịch tài liệu, gói \texttt{database} lưu trữ dữ liệu và gói \texttt{external\_services} đại diện cho các dịch vụ bên ngoài được tích hợp.

\textbf{a, Frontend}
\begin{itemize}
  \item \texttt{routes}: Xác định tuyến đường giao diện và điều hướng người dùng đến các trang tương ứng thông qua thành phần \texttt{App}.
  \item \texttt{pages}: Chứa các trang giao diện theo nhóm người dùng, quản trị và các trang chung như đăng nhập, dịch văn bản, lịch sử tệp, thư viện cá nhân, quản lý tài khoản và quản lý ngôn ngữ.
  \item \texttt{components}: Chứa các thành phần giao diện tái sử dụng như chọn ngôn ngữ, tải tệp, chuyển đổi định dạng và hiển thị thông báo.
  \item \texttt{services}: Là tầng trung gian gọi API backend, đảm nhiệm việc gửi yêu cầu và nhận dữ liệu trả về thông qua các module như \texttt{api}, \texttt{authService}, \texttt{translationService}, \texttt{keywordService} và \texttt{languageService}.
  \item \texttt{context\_hooks}: Quản lý trạng thái dùng chung ở phía frontend, tiêu biểu là trạng thái xác thực và danh sách ngôn ngữ theo công ty hoặc thư viện.
\end{itemize}

\textbf{b, Backend}
\begin{itemize}
  \item \texttt{urls}: Định tuyến các request API đến đúng view xử lý.
  \item \texttt{views}: Đóng vai trò Controller trong kiến trúc MVC, tiếp nhận request, kiểm tra dữ liệu, gọi serializer, model, service sau đó trả response cho frontend.
  \item \texttt{serializers}: Chuyển đổi dữ liệu giữa Django model và JSON để backend giao tiếp với frontend qua REST API.
  \item \texttt{services}: Chứa các xử lý nghiệp vụ/tích hợp dùng chung như upload tệp lên S3, quản lý glossary, gọi ConvertAPI và xử lý cấu hình ngôn ngữ theo công ty.
  \item \texttt{models}: Định nghĩa các bảng dữ liệu bằng Django ORM, gồm người dùng, công ty, ngôn ngữ, tệp đã dịch, thuật ngữ riêng, đề xuất thuật ngữ, thông báo và token làm mới.
\end{itemize}

\textbf{d, Database}
\begin{itemize}
  \item \texttt{database}: Lưu trữ dữ liệu bền vững của hệ thống. Frontend không truy cập trực tiếp database; mọi thao tác đọc/ghi đều đi qua backend và Django ORM.
\end{itemize}

\textbf{e, External services}
\begin{itemize}
  \item \texttt{external\_services}: Đại diện cho các dịch vụ ngoài được hệ thống tích hợp như Google Cloud Translation, Google Cloud Storage, Amazon S3, ConvertAPI và Azure Document Intelligence.
\end{itemize}

\section{Thiết kế chi tiết}
\subsection{Thiết kế giao diện}
Giao diện của hệ thống được thiết kế cho ứng dụng web nội bộ, sử dụng React SPA kết hợp Tailwind CSS. Các màn hình chính dùng chung một bố cục gồm sidebar bên trái, header phía trên và vùng nội dung có thanh cuộn riêng. Cách tổ chức này phù hợp với hệ thống.

\textbf{a, Thông số kỹ thuật}

Bảng dưới đây trình bày các thông số kỹ thuật chính được áp dụng trong thiết kế giao diện.

\begin{table}[H]
\centering
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|c|>{\raggedright\arraybackslash}p{0.3\linewidth}|>{\raggedright\arraybackslash}p{0.52\linewidth}|}
\hline
\textbf{STT} & \textbf{Thông tin} & \textbf{Chi tiết} \\ \hline
1 & Kiểu ứng dụng & Web SPA, xây dựng bằng React và định tuyến bằng React Router. \\ \hline
2 & Độ phân giải thiết kế tham chiếu & 1920 $\times$ 1080 px, phù hợp với màn hình desktop/laptop thường dùng trong môi trường văn phòng. \\ \hline
3 & Tỷ lệ khung hình tham chiếu & 16:9. \\ \hline
4 & Bố cục tổng thể & Sidebar cố định, header cố định, vùng nội dung cuộn độc lập. \\ \hline
5 & Kích thước sidebar & Mở rộng: 256 px; thu gọn: 64 px; trên mobile hiển thị dạng overlay rộng 256 px. \\ \hline
6 & Kích thước header & Cao 56 px, nằm phía trên vùng nội dung chính. \\ \hline
7 & Khoảng cách nội dung & Vùng nội dung chung có padding 8 px; các màn hình nghiệp vụ thường dùng padding 24 px. \\ \hline
8 & Màu nền và màu chủ đạo & Nền nội dung \#F8F8F8; khối nội dung màu trắng; trạng thái menu đang chọn dùng indigo đậm \#4338CA; chữ và viền dùng các sắc độ xám. \\ \hline
9 & Kiểu chữ & Sử dụng nhóm font sans-serif mặc định của Tailwind/browser; tiêu đề trang dùng cỡ lớn hơn, nhãn form dùng chữ nhỏ in hoa. \\ \hline
10 & Thông báo phản hồi & Sử dụng React Toastify, hiển thị ở góc trên bên phải, tự đóng sau 3000 ms và dùng theme màu theo trạng thái. \\ \hline
11 & Trạng thái responsive & Từ breakpoint \texttt{md}, sidebar hiển thị cố định; dưới breakpoint này, sidebar mở bằng nút menu trên header. \\ \hline
\end{tabular}
\caption{Thông số kỹ thuật màn hình}
\label{tab:ui-specification}
\end{table}

\textbf{b, Bố cục giao diện}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinhve/hinh_bo_cuc_tong_the.png}
\caption{Bố cục tổng quát của màn hình sau đăng nhập}
\label{fig:ui-main-layout}
\end{figure}

Bố cục chính của hệ thống gồm ba vùng: sidebar, header và content. Sidebar đặt ở bên trái, chứa logo, các nhóm menu chức năng và nút thu gọn. Header đặt ở phía trên vùng nội dung, hiển thị tên hệ thống, nút mở menu trên mobile, biểu tượng thông báo, thông tin người dùng và nút đăng xuất. Content là vùng làm việc chính, dùng để hiển thị từng màn hình nghiệp vụ theo route hiện tại. 

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinhve/header.png}
\caption{Bố cục header}
\label{fig:ui-main-layout}
\end{figure}


Sidebar được chia thành ba nhóm chức năng. Nhóm đầu gồm các chức năng xử lý tài liệu như dịch file, dịch văn bản, chuyển đổi định dạng và lịch sử file. Nhóm \texttt{Libraries} chứa thư viện thuật ngữ cá nhân và thư viện thuật ngữ chung. Nhóm \texttt{Admin} chỉ hiển thị với người dùng có vai trò quản trị, gồm quản lý tài khoản, thống kê từ khóa và quản lý ngôn ngữ. Mỗi mục menu có biểu tượng đi kèm, trạng thái đang chọn được làm nổi bật bằng màu indigo đậm và chữ trắng.
 
\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinhve/sidebar.png}
\caption{Bố cục sidebar}
\label{fig:ui-main-layout}
\end{figure}

\textbf{c, Quy chuẩn thiết kế thành phần giao diện}

Các thành phần giao diện được chuẩn hóa để bảo đảm tính nhất quán giữa các màn hình.

\begin{itemize}
  \item \textbf{Nút bấm:} Nút chính dùng màu indigo đậm, chữ trắng, bo tròn; nút phụ dùng nền trắng, viền xám hoặc indigo và đổi màu khi hover. Các nút dạng hành động chính như \textit{Translate}, \textit{Add Keyword}, \textit{Import}, \textit{Export} được đặt gần vùng nội dung cần thao tác.
  \item \textbf{Form và bộ lọc:} Các trường chọn ngôn ngữ, chọn thư viện thuật ngữ và bộ lọc dữ liệu được đặt trong khối nền trắng, viền mảnh, bo góc lớn. Nhãn trường nhập dùng chữ nhỏ, in hoa để dễ quét thông tin.
  \item \textbf{Vùng tải file:} Màn hình dịch file sử dụng vùng kéo thả có viền nét đứt, biểu tượng upload ở giữa, mô tả định dạng hỗ trợ và nút chọn file. Khi người dùng kéo file vào vùng upload, viền và nền đổi sang sắc indigo nhạt để phản hồi tương tác.
  \item \textbf{Bảng dữ liệu:} Các màn hình thư viện thuật ngữ, lịch sử file và quản trị sử dụng bảng có đường viền rõ, hàng tiêu đề nổi bật, hỗ trợ các thao tác như tìm kiếm, lọc, sửa, xóa, import và export.
  \item \textbf{Thông báo phản hồi:} Các thông báo thành công, lỗi hoặc cảnh báo được hiển thị bằng toast ở góc trên bên phải, không làm gián đoạn bố cục chính của màn hình.
  \item \textbf{Cuộn nội dung:} Toàn bộ ứng dụng dùng khung \texttt{h-screen}; phần content có thanh cuộn riêng, giúp sidebar và header luôn giữ vị trí ổn định khi người dùng thao tác với bảng hoặc nội dung dài.
\end{itemize}

\textbf{d, Màn hình minh họa}

Màn hình dịch file là màn hình chính của hệ thống. Bố cục gồm tiêu đề chức năng, mô tả ngắn, thanh chọn ngôn ngữ nguồn, ngôn ngữ đích, chế độ glossary và vùng upload tài liệu. Vùng upload hỗ trợ các định dạng PDF, DOCX, XLSX và PPTX, đồng thời cho phép người dùng bấm chọn file hoặc kéo thả file vào màn hình.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinhve/dich-vanban.png}
\caption{Màn hình minh họa chức năng dịch file}
\label{fig:ui-file-translation}
\end{figure}

Màn hình dịch văn bản có bố cục hai cột chính: vùng nhập văn bản nguồn và vùng hiển thị bản dịch. Phía trên là thanh chọn ngôn ngữ nguồn, ngôn ngữ đích và chế độ glossary. Cách bố trí này giúp người dùng dễ so sánh nội dung trước và sau dịch trên cùng một màn hình.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinhve/dich_text.png}
\caption{Màn hình minh họa chức năng dịch văn bản}
\label{fig:ui-text-translation}
\end{figure}

Màn hình thư viện thuật ngữ sử dụng bố cục thiên về bảng dữ liệu. Phía trên bảng là tiêu đề, các nút thao tác như thêm từ khóa, tải mẫu, import, export và vùng tìm kiếm. Bảng dữ liệu hiển thị thuật ngữ theo từng ngôn ngữ, kèm cột thao tác để chỉnh sửa, xóa hoặc gửi đề xuất vào thư viện chung.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinhve/dich-vanban.png}
\caption{Màn hình minh họa chức năng dịch file}
\label{fig:ui-file-translation}
\end{figure}

\subsection{Thiết kế lớp}
Để trình bày thiết kế lớp một cách rõ ràng trong báo cáo, các thành phần xử lý chính được trừu tượng hóa thành các lớp đại diện theo trách nhiệm của chúng trong hệ thống. Hai lớp được lựa chọn để mô tả gồm TranslationService, DocumentTranslationService. 

\textbf{a, Lớp TranslationService}

\begin{figure}[H]
\centering
\fbox{
\begin{minipage}{0.46\linewidth}
\centering
TranslationProcessor\\
\hrulefill\\[-0.2em]
\vspace{0.4em}
set\_translation\_context\\
clear\_translation\_context\\
preprocess\_brackets\\
postprocess\_brackets\\
translate\_text\_with\_glossary\\
translate\_texts\_batch\\
translate\_file
\vspace{0.4em}
\end{minipage}
}
\caption{Thiết kế chi tiết lớp TranslationService}
\label{fig:module-translate-text}
\end{figure}

Lớp TranslationServicelà khối xử lý dịch văn bản trung tâm của hệ thống, làm việc trực tiếp với Google Cloud Translation và quản lý ngữ cảnh sử dụng glossary theo từng luồng xử lý. Các phương thức chính của lớp gồm:

\begin{itemize}
  \item set\_translation\_context: Thiết lập ngữ cảnh dịch theo từng request, gồm chế độ thư viện thuật ngữ và ID người dùng. Ngữ cảnh này giúp backend chọn đúng glossary chung, glossary riêng hoặc không dùng glossary.
  \item clear\_translation\_context: Xóa ngữ cảnh dịch sau khi hoàn thành xử lý, tránh việc request sau bị ảnh hưởng bởi dữ liệu của request trước.
  \item preprocess\_brackets: Tiền xử lý văn bản trước khi gửi sang Google Cloud Translation, đặc biệt với nội dung nằm trong dấu ngoặc nhọn.
  \item postprocess\_brackets: Hậu xử lý kết quả dịch để khôi phục lại nội dung đã được biến đổi ở bước tiền xử lý.
  \item translate\_text\_with\_glossary: Dịch một đoạn văn bản với glossary nếu có. Khi glossary lỗi hoặc không tồn tại, hàm chuyển sang dịch tiêu chuẩn để hệ thống vẫn trả được kết quả.
  \item translate\_texts\_batch: Dịch danh sách nhiều chuỗi văn bản theo từng lô. Hàm này được các module xử lý file sử dụng để giảm số lần gọi dịch riêng lẻ.
  \item translate\_file: Hàm xử lý chung cho các định dạng file dạng nén như XLSX và PPTX. Hàm giải nén file, xác định glossary phù hợp, gọi hàm dịch XML tương ứng, dịch ảnh nếu có, sau đó nén lại thành file kết quả.
\end{itemize}

\textbf{b, Lớp DocumentTranslationService}

\begin{figure}[H]
\centering
\fbox{
\begin{minipage}{0.5\linewidth}
\centering
DocumentTranslationService\\
\hrulefill\\[-0.2em]
\vspace{0.4em}
translate\_docx\\
pdf\_to\_docx\\
docx\_to\_pdf\\
translate\_pdf\_ocr\\
translate\_xlsx\\
translate\_pptx\\
translate\_all\_xml\_in\_folder\\
merge\_all\_paragraphs\\
cached\_translate\_text
\vspace{0.4em}
\end{minipage}
}
\caption{Thiết kế chi tiết lớp DocumentTranslationService}
\label{fig:module-document-translation}
\end{figure}

Lớp DocumentTranslationService gồm các phần xử lý DOCX, XLSX, PPTX, PDF và PDF/OCR. Lớp này chịu trách nhiệm bảo toàn cấu trúc file trong quá trình dịch. Thay vì chỉ dịch văn bản thuần, lớp phải tách nội dung từ file tài liệu, dịch nội dung cần thiết rồi ghi ngược lại vào cấu trúc file ban đầu.

\begin{itemize}
  \item translate\_docx: Hàm xử lý dịch file DOCX. Hàm tự phát hiện ngôn ngữ nguồn nếu chưa được truyền vào, sau đó thử phương pháp dịch truyền thống; nếu lỗi thì chuyển sang phương pháp strict.
  \item pdf\_to\_docx: Hàm chuyển đổi PDF sang DOCX bằng pdf2docx, giúp hệ thống tận dụng lại luồng dịch DOCX đối với những PDF có thể chuyển đổi được.
  \item docx\_to\_pdf: Hàm chuyển đổi DOCX sau xử lý trở lại PDF bằng docx2pdf.
  \item translate\_pdf\_ocr: Hàm xử lý PDF theo hướng OCR. Hàm gọi Azure Document Intelligence để phân tích bố cục, trích xuất nội dung, dịch các đoạn văn bản, dựng lại PDF từ ảnh nền và thêm textbox bản dịch lên tài liệu.
  \item translate\_xlsx: Hàm xử lý dịch file XLSX. Hàm phát hiện ngôn ngữ nguồn khi cần, rồi gọi hàm xử lý chung translate\_file với loại file là xlsx.
  \item translate\_pptx: Hàm xử lý dịch file PPTX. Hàm phát hiện ngôn ngữ nguồn, sau đó gọi translate\_file với loại file là pptx.
  \item translate\_all\_xml\_in\_folder: Hàm quét các file XML bên trong tài liệu, thu thập các đoạn text cần dịch, dịch theo batch và ghi kết quả vào lại XML.
  \item merge\_all\_paragraphs: Hàm gộp các đoạn text bị tách thành nhiều run trong XML, giúp quá trình dịch giữ được ngữ nghĩa tốt hơn.
  \item cached\_translate\_text: Hàm bọc lớp cache cho việc dịch các đoạn text lặp lại, giúp giảm số lần gọi dịch với cùng một nội dung.
\end{itemize}

\textbf{d, Biểu đồ trình tự cho một số chức năng quan trọng}

\textbf{Dịch tệp tài liệu}

Luồng dịch tệp bắt đầu khi người dùng upload file và chọn ngôn ngữ đích. Hệ thống tiếp nhận yêu cầu, kiểm tra dữ liệu đầu vào, gọi các dịch vụ bên ngoài để tải file, dịch nội dung, xử lý OCR hoặc chuyển đổi định dạng khi cần. Sau khi xử lý xong, hệ thống lưu lịch sử và trả về đường dẫn file đã dịch cho người dùng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{Hinhve/activity_diagram.png}
    \caption{Biểu đồ trình tự: Dịch tệp tài liệu}
    \label{fig:activity-diagram-file}
\end{figure}

\textbf{Đề xuất và phê duyệt thuật ngữ}

Người dùng gửi thuật ngữ từ thư viện cá nhân lên thư viện chung. Sau khi đề xuất được tạo ở trạng thái \texttt{pending}, Library Keeper có thể phê duyệt. Khi phê duyệt thành công, hệ thống cập nhật dữ liệu nội bộ, đồng bộ glossary chung và tạo thông báo cho người dùng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{Hinhve/activity_diagram2.png}
    \caption{Biểu đồ trình tự: Đề xuất và phê duyệt thuật ngữ}
    \label{fig:activity-diagram-keyword}
\end{figure}

\subsection{Thiết kế cơ sở dữ liệu}
Phần này có độ dài từ hai đến bốn trang. Sinh viên thiết kế, vẽ và giải thích biểu đồ thực thể liên kết (E-R diagram). Từ đó, sinh viên thiết kế cơ sở dữ liệu tùy theo hệ quản trị cơ sở dữ liệu mà mình sử dụng (SQL, NoSQL, Firebase, v.v.)

Agent:

Cơ sở dữ liệu có thể dùng SQLite cho môi trường phát triển và PostgreSQL cho môi trường triển khai. Các thực thể chính gồm người dùng, token làm mới, file dịch, thuật ngữ riêng, đề xuất thuật ngữ, thiết lập hàng đợi thư viện, ngôn ngữ, công ty, ngôn ngữ theo công ty, thông báo và nhật ký thao tác ngôn ngữ. Quan hệ dữ liệu nổi bật gồm một người dùng có nhiều file dịch, nhiều từ khóa riêng, nhiều đề xuất thuật ngữ và nhiều refresh token; một công ty có nhiều người dùng và nhiều cấu hình ngôn ngữ; một cấu hình ngôn ngữ theo công ty liên kết một công ty với một ngôn ngữ; một nhật ký thao tác ghi lại người thực hiện, công ty, ngôn ngữ và hành động.

\begin{verbatim}
erDiagram
    CustomUser ||--o{ TranslatedFile : owns
    CustomUser ||--o{ PrivateKeyword : owns
    CustomUser ||--o{ KeywordSuggestion : submits
    CustomUser ||--o{ RefreshToken : has
    Company ||--o{ CustomUser : includes
    Company ||--o{ CompanyLanguage : configures
    Language ||--o{ CompanyLanguage : enabled_as
    KeywordSuggestion ||--o{ PrivateKeyword : linked_from
    CustomUser ||--o{ LanguageAuditLog : acts
    Company ||--o{ LanguageAuditLog : audited
    Language ||--o{ LanguageAuditLog : audited
\end{verbatim}

\section{Xây dựng ứng dụng}
\subsection{Thư viện và công cụ sử dụng}
Sinh viên liệt kê các công cụ, ngôn ngữ lập trình, API, thư viện, IDE, công cụ kiểm thử, v.v. mà mình sử dụng để phát triển ứng dụng. Mỗi công cụ phải được chỉ rõ phiên bản sử dụng. SV nên kẻ bảng mô tả tương tự như Bảng \ref{table:my_label}. Nếu có nhiều nội dung trình bày, sinh viên cần xoay ngang bảng.

Agent:

Danh sách công cụ và thư viện chính gồm Python, Node.js, Nginx, Django, Django REST Framework, SimpleJWT, boto3, Google Cloud Translation, Google Cloud Storage, các thư viện xử lý PDF, DOCX, XLSX, PPTX, ảnh, dữ liệu bảng và ConvertAPI. Agent: [NEED\_MANUAL\_REVIEW] Cần bổ sung bảng phiên bản chính xác của các thư viện khi hoàn thiện tài liệu kỹ thuật.

\begin{table}[H]
\centering{}
    \begin{tabular}{lll}
        \hline
        \textbf{Mục đích} & \textbf{Công cụ}       & \textbf{Địa chỉ URL}    \\ \hline
        IDE lập trình     & Eclipse Oxygen a64 bit & http://www.eclipse.org/ \\ \hline
        v.v.              & v.v.                   & v.v.                    \\ \hline
        \end{tabular}
    \caption{Danh sách thư viện và công cụ sử dụng}
    \label{fig:my_label}
\end{table}

\subsection{Kết quả đạt được}
Sinh viên trước tiên mô tả kết quả đạt được của mình là gì, ví dụ như các sản phẩm được đóng gói là gì, bao gồm những thành phần nào, ý nghĩa, vai trò?

Sinh viên cần thống kê các thông tin về ứng dụng của mình như: số dòng code, số lớp, số gói, dung lượng toàn bộ mã nguồn, dung lượng của từng sản phẩm đóng gói, v.v. Tương tự như phần liệt kê về công cụ sử dụng, sinh viên cũng nên dùng bảng để mô tả phần thông tin thống kê này.

Agent:

Kết quả xây dựng thể hiện qua ba nhóm sản phẩm chính: giao diện web, backend API và khối dịch tài liệu. Backend cung cấp API cho dịch văn bản, dịch file, upload S3, chuyển đổi PDF, lịch sử file, thông báo, thuật ngữ, người dùng, ngôn ngữ, health check và thống kê. Frontend có các màn hình nghiệp vụ tương ứng. Khối dịch tài liệu xử lý DOCX, XLSX, PPTX, PDF/OCR, ảnh, phát hiện ngôn ngữ và tạo glossary. Agent: [NEED\_MANUAL\_REVIEW] Cần bổ sung thống kê chính thức về số dòng mã, số module và dung lượng sản phẩm đóng gói nếu báo cáo yêu cầu.

\subsection{Minh họa các chức năng chính}
Sinh viên lựa chọn và đưa ra màn hình cho các chức năng chính, quan trọng, và thú vị nhất. Mỗi giao diện cần phải có lời giải thích ngắn gọn. Khi giải thích, sinh viên có thể kết hợp với các chú thích ở trong hình ảnh giao diện.

Agent:

Các chức năng chính cần minh họa bằng ảnh giao diện khi hoàn thiện báo cáo gồm màn hình upload và dịch file, màn hình dịch văn bản, màn hình kết quả dịch, màn hình lịch sử file, màn hình thư viện thuật ngữ riêng, màn hình quản trị thư viện chung, màn hình thống kê từ khóa và màn hình quản lý ngôn ngữ. Agent: [NEED\_MANUAL\_REVIEW] Cần bổ sung ảnh chụp màn hình các chức năng này.

\section{Kiểm thử}
Phần này có độ dài từ hai đến ba trang. Sinh viên thiết kế các trường hợp kiểm thử cho hai đến ba chức năng quan trọng nhất. Sinh viên cần chỉ rõ các kỹ thuật kiểm thử đã sử dụng. Chi tiết các trường hợp kiểm thử khác, nếu muốn trình bày, sinh viên đưa vào phần phụ lục.
Sinh viên sau cùng tổng kết về số lượng các trường hợp kiểm thử và kết quả kiểm thử. Sinh viên cần phân tích lý do nếu kết quả kiểm thử không đạt.

Agent:

Kế hoạch kiểm thử cần tập trung vào các chức năng quan trọng gồm thống kê từ khóa, dịch file, upload S3, chuyển đổi định dạng, quản lý glossary và xác thực người dùng. Với API thống kê từ khóa, các trường hợp kiểm thử cần bao gồm quyền truy cập của admin, chặn người dùng thường và người chưa xác thực, kiểm tra tham số ngày bắt buộc, định dạng ngày, thứ tự ngày bắt đầu/kết thúc, và cấu trúc dữ liệu trả về. Agent: [NEED\_MANUAL\_REVIEW] Cần rà soát và cập nhật bộ test tự động trước khi kết luận toàn bộ test pass.
\section{Triển khai}
Sinh viên trình bày mô hình và/hoặc cách thức triển khai thử nghiệm/thực tế. Ứng dụng của sinh viên được triển khai trên server/thiết bị gì, cấu hình như thế nào. Kết quả triển khai thử nghiệm nếu có (số lượng người dùng, số lượng truy cập, thời gian phản hồi, phản hồi người dùng, khả năng chịu tải, các thống kê, v.v.)

Agent:

Hệ thống được triển khai theo mô hình container gồm backend, frontend và Nginx. Backend chạy ở cổng nội bộ, frontend được build thành tài nguyên tĩnh và phục vụ qua Nginx, còn reverse proxy chính mở cổng HTTP/HTTPS, dùng chứng chỉ TLS, chuyển HTTP sang HTTPS, định tuyến request API tới backend và các request còn lại tới frontend. Agent: [NEED\_MANUAL\_REVIEW] Cần bổ sung log triển khai, số lượng người dùng, thời gian phản hồi hoặc kết quả kiểm thử tải.

% Agent: BEGIN
\medskip
\textbf{Kết chương 4.} Chương này đã trình bày thiết kế kiến trúc MVC với frontend tách rời của hệ thống, bao gồm React SPA ở vai trò View, backend Django REST Framework ở vai trò Controller và Django ORM ở vai trò Model. Thiết kế cơ sở dữ liệu xác định mười một thực thể chính với quan hệ rõ ràng, trong đó người dùng là trung tâm liên kết tới file dịch, thuật ngữ riêng, đề xuất thuật ngữ và cấu hình công ty. Quá trình xây dựng tạo ra ba thành phần có thể triển khai độc lập là giao diện web, backend API và hệ thống container. Quy trình dịch file được kiểm thử qua luồng API thống kê từ khóa, xác thực phân quyền và xử lý tham số đầu vào. Hệ thống được triển khai theo mô hình container Nginx trên tên miền có HTTPS. Các đóng góp kỹ thuật nổi bật sẽ được phân tích chi tiết trong Chương 5.
% Agent: END

\end{document}
